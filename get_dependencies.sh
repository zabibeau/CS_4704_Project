#This is a script to install the dependencies for the project
#Most of these will be pip installed so be sure to have pip installed

pip install spotipy
pip install os
pip install python-dotenv
pip install requests
pip install urllib
pip install json
pip install base64
pip install webbrowser
pip install flask
pip install ytmusicapi
